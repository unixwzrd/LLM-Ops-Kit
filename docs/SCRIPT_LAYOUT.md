# Script Layout (Canonical)

Canonical script home:
- `~/projects/agent-work/bin`
- `~/projects/agent-work/scripts/openclaw`
- `~/projects/agent-work/scripts/workspace`

Compatibility paths:
- `~/bin/*` entries are symlinked to `~/projects/agent-work/bin/*`.

Notes:
- `.openclaw` and `OpenClaw-workspace` remain local operational repos.
- Canonical docs/planning/scripts now live under `agent-work`.
- Old `~/bin` git metadata is retired as `.git.retired-<timestamp>`.
